---
comments: true
---

# Start a Web Server To Serve Local Pages

The command below will start an http server that will serve pages from a local folder at `localhost:8000`. This is helpful in visualizing local website builds. 

```bash
python3 -m http.server 8000
```